{
  "version": 2,
  "builds": [{ "src": "src/handler.ts", "use": "@now/node" }],
  "routes": [{ "src": "/", "dest": "src/handler.ts" }],
  "env": {
    "NODE_ENV": "production",
    "APP_ID": "@moai-dev-app-id",
    "WEBHOOK_SECRET": "@moai-webhook-secret",
    "PRIVATE_KEY": "@moai-dev-private-key"
  }
}
